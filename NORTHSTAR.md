# North Star

> 构建一个可观察、可评估、可持续改进的原生 Agent 团队运行时，在保持交付质量与人工可参与性的前提下，持续降低协作成本。

2026-09-19 用户确认。模型分层与角色记忆仍是基础机制；Trace、Metrics、半自动 Eval 与受控改进 Loop 成为下一阶段重点。这是产品方向，不代表 RSI 已经实现或已证明收益。

## 我们为什么做这个项目

让用户通过可监督、可验收、可持续改进的 Agent 团队，降低总 Token、交付历时和人工纠偏负担，持续获得不低于约定验收标准的任务交付质量。与单一顶级 Agent 的同标准对照仍是需要验证的比较，不是已得到的结论。

Agent Teams 是实现方式，不是目的。成员更多、窗口更多、并行更多，都不天然代表更高效率。我们要把顶级模型的能力用在最值得的地方，并让用户能够长期放心地把任务交给团队。

## 核心机制

- **Manager 把关：** 理解需求、拆解任务、处理关键决策、协调依赖、持续审查和独立验收，对最终结果负责，而不是投递后即结束。
- **Worker / Subagent 执行：** 使用适合任务的较低成本模型，在明确范围内实现、测试、构建或部署；提供真实证据，提交不等于验收。
- **用户可见、可控：** 能知道谁在做什么、进展和阻塞是什么，并保留目标、重大取舍和权限决定权。Liaison 支持沟通与进度查询，不成为第二个调度者。
- **基于证据改进：** Trace 保留事实，Metrics 衡量成本与结果，半自动 Eval 提出可验证假设，Loop 通过单项试用决定保留或回退。删除冗余规则和封装机械步骤，与增加新能力同样属于改进。

正式成员使用独立原生任务，用户可直接交流；Worker 在授权范围内可使用临时子 Agent，Manager/Liaison 不创建或指挥临时子 Agent。不为简单任务机械地增加团队层级。模型分层原则仍是“顶级模型把关，合适模型执行”，而非以更多窗口或调用作为成果。

顶级 Manager 是推荐配置，不是强制配置。Manager 的模型与推理强度始终由用户指定，复用成员不擅自改配置；执行成员遵守既定模型策略，临时子 Agent 不超过直接父 Agent 的模型等级。具体型号默认值由操作契约维护，不在北极星中另建一套配置。

## 当前聚焦

当前专注 **GPT‑6 + Codex**，先把这一环境中的真实、长期、多任务协作做可靠。

优先完善需求到派发、执行、提交、审查、返工和验收的闭环，以及跨回合恢复和异常处理。Skill 与轻量运行层服务于这个目标，不重新建设模型执行引擎。

下一阶段先在 Team 内部完成可复现的任务时间线、指标与单变量改进实验，再按实际需要对接外部 Trace/Eval Runtime。参见 [RSI 设计](docs/design/team-rsi.md)、[任务时间线](docs/design/task-timeline.md) 与 [优化台账](docs/optimization/team-flow-experiments.md)。

这一定位是适配方向，不代表所有宿主能力已经接入或验证。当前能力与限制以 [README](README.md) 和 [运行说明](docs/runtime-usage.md) 为准；不急于抽象成通用多模型、多宿主平台。

## 如何判断是否接近北极星

**先通过质量门槛，再比较总消耗。** “不降低质量”是必须验证的目标，不是多 Agent 协作天然具备的保证。

以单一顶级 Agent 为基线，使用相同任务范围、起始材料、工具权限与验收标准，对比团队模式。评估前固定成功条件、质量检查和复验方式；不能看到结果后降低验收标准。

- **质量门槛：** 相同标准下的任务验收通过情况、缺陷与回归情况，以及必要的人工作业验收。没有证据时标记未知，不能用 Worker 自报完成或 Manager 自评替代验证。
- **核心消耗指标：** 相同质量门槛下，每个验收完成任务的团队总 Token 消耗。统计所有成员、上下文重复、协调、审查、返工、失败尝试与无效监控；同时保留任务集总消耗和失败率，不能通过排除失败任务来美化结果。
- **配套指标：** 实际费用、端到端交付耗时、人工纠偏负担与协调开销。不同模型的单价不同：模型更便宜不等于 Token 更少，Token 减少也不自动等于费用下降，二者分别报告。
- **改进闭环指标：** 每次实验的基线、唯一差异、实际生效版本、可比样本、质量退化和保留/回退结果。采集、评估、改写、回归与维护本身的成本也计入；不能只算业务任务节省、不算 RSI 开销。

比较时记录模型、推理强度、工具环境、采样范围和实际加载版本，重复测试代表性任务；不能把一次简单成功外推为所有任务上的收益。目前的功能测试和隔离实测证明了部分协作流程，不是“节省 Token 且质量不降”的对照实验。已有部分指标采集与本地基线，但自动逐步骤时间线与 RSI 闭环仍待实现；尚不承诺节省比例。

## 决策原则

1. **质量是约束，不是换取低消耗的筹码。** 不通过跳过验证、隐藏失败、提前验收或缩小已批准范围来省 Token。
2. **优化整个团队，而不是一个调用。** 较低成本模型导致反复返工、Manager 重做或上下文膨胀时，需要重新评估分工；升级模型须遵守用户配置和授权边界。
3. **减少无效活动，保留必要监督。** 定时器默认关闭，优先完成/阻塞通知和用户主动查询；可选定时器单次最多 24 小时，续期需人工确认。角色长期存在不等于模型长期空转。
4. **协作可恢复、可追溯。** 忙碌 Worker 不被独立新任务抢占；未知送达不盲重发；范围、占用、交接和验收依据能在后续回合恢复。
5. **可视化辅助监督，不替代核心能力。** HTML 看板帮助理解真实状态和进度，不能成为第二份业务事实，也不先于 Skill 的可靠交付闭环。
6. **单变量、可归因、可回退。** 每次只启用一个优化；相同验收标准下比较效果。不把时间邻近当因果，不把未知当零，不删除失败样本。
7. **改进必须有净收益。** 没有证据可以不改；不以自我改进为由无限反思、增加提示、轮询或未经授权修改生产 Skill。正常业务交付不等待 Eval 完成。

## 暂时不追求

- 为了展示 Agent Teams 而增加成员、窗口、消息或递归委派。
- 通用项目管理平台、自建常驻调度服务，或同时适配所有模型与宿主。
- 无人参与的无限自治、未经授权的模型升级，或以降低成本为理由扩大操作权限。
- 没有对照证据的“更省、更快、质量相同”宣传。

面对新功能，先问：它是否帮助团队在保持验收质量的前提下降低总消耗，或消除阻碍这一目标的可靠性、可监督性问题？如果没有明确关联，暂缓。

本文件指导产品取舍，不改变用户已确认的模型、权限、定时器或任务所有权契约，也不自动启用角色、任务或后台运行。
