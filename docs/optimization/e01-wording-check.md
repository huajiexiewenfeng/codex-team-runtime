# E01 源码澄清与离线措辞测试

状态：源码已改；2026-09-22 用户授权后已将 E01 单一差异全局安装。未提交推送；真实任务加载与效果待观察。

## 唯一行为差异

只替换 `skills/manager-session/references/completion-notification.md` 中批准门禁的说明段：门禁限制业务推进，不要求 Worker 保持当前轮次运行。成功提交且精确 attempt 的 delivered 结果已记录、无剩余工作或明确主动等待要求时，以 submitted 待审结束当前轮次，保留占用；其他路径沿用原规则。

没有改模型、通知数量、发送协议、运行时状态机、验收标准、权限或重试规则。工作区还有其他历史未提交改动，不能将整个 Git diff 当作 E01 补丁或整体安装为单变量实验。回退仅还原上述段落，并核对后续编辑，不能覆盖整份文件。

本轮文件 SHA256：

- 修改前：`94729bc7f849934163b6b580b3b7a0115e648b5542557bde54cc6826959b979f`
- 修改后：`2521753ee846f1212b39dbd5bd148589034c6b0a53e0f6878f24773ce9258a3f`

文件哈希只标记本轮测试输入，不代表已安装或历史真实任务加载版本。

## 固定场景与方法

5 个独立新上下文样本读取修改前完整 completion-notification 引用文件；另 5 个独立样本读取修改后同一文件。控制组未包含拟议澄清，不是完全无规则的 Agent。各样本无主任务历史，不提供预期选项或根因判断。只读且禁止实际发送/等待工具，手工复核每份输出；这是决策模拟，不是运行中的真实团队实验。

共同场景：本地包已验证、submit 成功、精确 Manager 通知送达且同次 notice-result 版本 28 记录成功；Manager 未回复、任务 submitted、Worker 占用。原派发为“完成打包、汇报并等待 Manager 验收”，无其他工作。叠加交付速度、闭环责任和通知已耗时较久的背景，请样本给出下一实际动作或最终回复。

| 样本 | 修改前动作 | 修改后动作 |
| --- | --- | --- |
| a | 调用 wait_threads 等 Manager | 结束本轮，保持待审和占用 |
| b | 结束本轮，保持待审和占用 | 结束本轮，保持待审和占用 |
| c | 结束本轮，保持待审和占用 | 结束本轮，保持待审和占用 |
| d | 调用一次 wait_threads 等 Manager | 结束本轮，保持待审和占用 |
| e | 结束本轮，保持待审和占用 | 结束本轮，保持待审和占用 |

可追溯到当前开发任务的临时测试子代理 `e01_control_a` 至 `e01_control_e`、`e01_variant_a` 至 `e01_variant_e`。保留原生任务输出，未复制隐藏推理。

基线失败原文：

> a：下一步：调用原生 `wait_threads`，等待已绑定的精确 Manager 验收反馈，保留等待游标。

> d：下一步：描述性调用 `wait_threads`，对已绑定的精确 Manager 等待一次状态事件，返回后核实是否有验收决定。

修改后 a 原文：

> 现在结束本轮，不重复通知、不轮询；保留 submitted 和 Worker 占用。最初“等待验收”未明确要求本轮持续主动等待。

5 次中 2 次选择等待 → 5 次中 0 次选择等待，仅说明这些有限场景的解释更一致，不是统计显著性、所有模型遵循率或端到端节省比例。测试消耗未单独归属 Token，不能填零或忽略改进成本。

## 边界复核

另一个独立样本 `e01_boundaries` 处理三个反例：

- 明确要求当前轮次主动等待最多 30 秒：选择按要求等待，而非默认结束。
- policy-denied：保留拒绝、停止该发送、不改写或换渠道重发，区分已提交与未送达。
- 第二个制品仍待验证：继续原授权校验，不把首个提交完成当作整体完成。

12 个确定性 completion-exit 评估用例亦通过。它们验证辅助分类，不能替代上面的行为样本或后续真实任务观察。

通用 Python quick_validate 未运行成功：当前所选 Python 缺少 PyYAML，未为此安装依赖。入口 frontmatter 未修改；使用仓库现有 Skill 测试与 diff 格式检查补充验证，不能将其称为通用校验器通过。

仓库 `manager-session-skill` 与 `task-eval-completion` 两组共 17 项测试通过。初次的子进程测试被沙箱 EPERM 阻止；经工具批准在沙箱外重跑同两组后全部通过，未改测试来规避失败。差异格式检查通过。

## 安装与试用边界

用户于 2026-09-22 选择全局安装，并要求可回退，替代此前限定固件团队的建议。20:35:56 +08:00 安装完成；共享 manager-session 共 11 个文件，仅 completion-notification.md 变化，其余哈希不变。旧文件及新文件固定副本、校验式安装/回退脚本和记录保存在开发工作区 `artifacts/e01-global-install-20260922/`。临时副本已实际演练安装和回退，恢复哈希一致；后续修改会使回退脚本停止，避免覆盖新工作。

E01 已部署待观察，不中断在途 Worker、不声称所有会话已加载。开始采样时记录具体任务和实际读取版本；初次任务只作检查点，不凭一个成功样本保留实验。E02–E06 不同时启用，不创建计时器。磁盘安装或回退均不会自动改写已有会话上下文。
