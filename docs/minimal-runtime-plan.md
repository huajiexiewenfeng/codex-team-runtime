# 最小运行层实施计划

状态：SUBMITTED，待父任务验收。五步已实现并完成离线验证；详见 [提交证据](submission-evidence.md)。

依据：本次已批准的最小纵向切片范围、README、V1 scope、Manager Session 设计；临时只读检查了原仓库历史路线、已确认 HTML 草图及入口/前置检查说明。后来的实现授权优先于历史 R0-only 路线。

1. 用 Node 标准库建立严格校验的 JSON 状态、版本检查、互斥写入及原子替换；团队、成员与轮次独立。
2. 以显式事件推进分配、提交、审查、返工、复验、批准、阻塞、观察和轮次关闭；记录必要来源摘要，不读取会话日志。
3. 从同一不可变快照派生 Liaison JSON 与只读 HTML，时间戳缺失/陈旧明确显示；所有导航禁用。
4. CLI 提供 init/apply/snapshot/render/demo。离线夹具不得冒充宿主观察；停报只计算意图，离线回执不能确认宿主暂停。
5. 验证输入及坏文件保护、身份缺失、审查闭环、计时、轮次隔离、停报语义、HTML 转义及只读一致性；更新实际范围与使用说明。

写入范围仅此 worktree。无服务器、第三方依赖、Skill/hook 安装、协议注册、任务创建、自动化、发布或真实浏览器测试。身份与权限字段是本地记录契约，不构成宿主认证层。宿主导航/观察/共享状态权限/定时实际暂停留待现场验证。
